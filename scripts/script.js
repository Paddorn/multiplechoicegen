function submitQuestionAmount()
{
    const choice = ["A", "B", "C", "D"];
    const numQuestions = document.querySelector('#numQuestions').value;
    const answerList= [];

    for (let i=0; i<numQuestions; i++){
        answerList.push(i);
        for(let i=0; i<answerList.length-1; i++){
            
        }

    }

}
